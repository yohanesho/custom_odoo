<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="custom_sale_order_form" model="ir.ui.view">
        <field name="name">custom.sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="priority" eval="5"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_confirm' and @states='sent']" position="replace">
                <button name="action_confirm" states="sent" string="Confirm Sale" group="sales_team.group_sale_manager" class="btn-primary o_sale_confirm" type="object"/>
            </xpath>
            <xpath expr="//button[@name='action_confirm' and @states='draft']" position="replace">
                <button name="action_confirm" states="draft" string="Confirm Sale" class="o_sale_confirm" type="object" group="sales_team.group_sale_manager"/>
            </xpath>

            <field name="user_id" position="after">
                <field name="approved_user_id" options="{'no_create': True}"/>
            </field>
        </field>
    </record>
    
    <record id="custom_sale_quotation_tree" model="ir.ui.view">
        <field name="name">custom.sale.quotation.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <xpath expr="/tree" position="replace">
                <tree string="Quotation" class="o_sale_order" decoration-bf="message_needaction==True" decoration-muted="state=='cancel'" decoration-danger="state in ['draft','sent'] and confirmation_date &lt; current_date">
                    <field name="message_needaction" invisible="1"/>
                    <field name="name" string="Quotation Number"/>
                    <field name="date_order"/>
                    <field name="partner_id"/>
                    <field name="user_id"/>
                    <field name="amount_total" sum="Total Tax Included" widget="monetary"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="confirmation_date" invisible="1"/>
                    <field name="state"/>
                </tree>    
            </xpath>
        </field>
    </record>

    <record id="custom_sale_order_tree" model="ir.ui.view">
        <field name="name">custom.sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <xpath expr="/tree" position="replace">
                <tree string="Sales Orders" decoration-bf="message_needaction==True" decoration-muted="state=='cancel'" decoration-danger="expired_delivery==True">
                    <field name="message_needaction" invisible="1"/>
                    <field name="name" string="Quotation Number"/>
                    <field name="date_order"/>
                    <field name="partner_id"/>
                    <field name="user_id"/>
                    <field name="amount_total" sum="Total Tax Included" widget="monetary"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="confirmation_date" invisible="1"/>
                    <field name="invoice_status"/>
                    <field name="state" invisible="1"/>
                    <field name="expired_delivery" invisible="1"/>
                </tree>    
            </xpath>
        </field>
    </record>
</odoo>