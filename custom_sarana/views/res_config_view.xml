<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="custom_view_account_config_settings" model="ir.ui.view">
        <field name="name">Account Settings</field>
        <field name="model">account.config.settings</field>
        <field name="inherit_id" ref="account.view_account_config_settings"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="after">
                <div>
                    <label for="rounding_account_id"/>
                    <field name="rounding_account_id" options="{'no_create': True}" class="oe_inline"/>
                </div>
            </field>
        </field>
    </record>
    <record id="custom_view_sale_config_settings" model="ir.ui.view">
        <field name="name">Sale Settings</field>
        <field name="model">sale.config.settings</field>
        <field name="inherit_id" ref="sale.view_sales_config"/>
        <field name="arch" type="xml">
            <field name="sale_note" position="before">
                <field name="user_approval_order" options="{'no_create': True}" widget="many2many_tags"/>
            </field>
        </field>
    </record>
</odoo>